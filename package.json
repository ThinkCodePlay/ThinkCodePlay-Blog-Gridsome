{
  "name": "ThinkCodePlay",
  "private": true,
  "scripts": {
    "win:build": "set NODE_OPTIONS=--openssl-legacy-provider && gridsome build",
    "win:dev": "set NODE_OPTIONS=--openssl-legacy-provider && gridsome develop",
    "mac:build": "export NODE_OPTIONS=--openssl-legacy-provider && gridsome build",
    "mac:dev": "export NODE_OPTIONS=--openssl-legacy-provider && gridsome develop",
    "explore": "gridsome explore"
  },
  "dependencies": {
    "@gridsome/remark-prismjs": "^0.5.0",
    "@gridsome/source-filesystem": "^0.6.2",
    "@gridsome/transformer-remark": "^0.6.4",
    "bootstrap": "^5.2.3",
    "bootstrap-vue": "^2.23.1",
    "gridsome": "^0.7.0",
    "vue": "^2.7.14"
  }
}
